module Ruboty
  module Handlers
    class Clock < Base
      on(
        /’º£’²¿’»þ/,
        name: "clock",
        description: "’¸½’ºß’»þ’¹ï’¤ò’ÊÖ’¤·’¤Þ’¤¹"
      )

      def clock(message)
        now = Time.now
        message.reply("#{now.hour}:#{now.min}")
      end
    end
  end
end